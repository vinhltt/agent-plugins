# Changelog

## 0.1.0 - 2026-06-03

### Added

- `excalidraw`: port diagram generation skill with MCP canvas and file-based rendering workflows.
- File-based render helper with `uv` + Playwright metadata.
- `excalidraw/scripts/install.sh`: one-command setup for the file-based renderer when MCP canvas is unavailable.
- Local SVG layout best-practices reference so the skill does not depend on `/ck:tech-graph`.
- Fix typo and malformed anti-pattern row in SVG layout reference.
